// ok, not quite a toolbox yet, not even a singleton, but we'll get there (if needed)

var pool;
var $form; // jQuery's
var $aigui;
var rightmost;
var timeout;
var poolGameState;

var $poolStateArea;
var $poolStateLink;

// from getPositions
var marioX;
var marioY;
